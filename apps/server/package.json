{
  "name": "@car-ball/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@car-ball/protocol": "0.1.0",
    "@car-ball/sim": "0.1.0"
  },
  "scripts": {
    "build": "npx tsc -p tsconfig.json",
    "typecheck": "npx tsc --noEmit -p tsconfig.json",
    "test": "echo \"No tests yet for @car-ball/server\""
  }
}
